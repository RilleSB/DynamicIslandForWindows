<Window x:Class="DynamicIslandPC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        Title="Dynamic Island PC" 
        Width="115" Height="60"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="ControlButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid ClipToBounds="False">
        <Border x:Name="IslandBorder" 
                CornerRadius="35"
                ClipToBounds="False">
            <Border.Background>
                <SolidColorBrush Color="#000000" Opacity="0.7"/>
            </Border.Background>
        
        <Grid>
            <!-- Режим паузы - маленький pill с иконкой паузы -->
            <Grid x:Name="PausedMode" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⏸" Foreground="White" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
            
            <!-- Минимальный режим - иконка + гифка -->
            <Grid x:Name="MinimalMode" Visibility="Visible">
                <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Grid Grid.Column="0">
                        <Ellipse Width="50" Height="50" Fill="Gray"/>
                        <Image x:Name="AlbumArtMinimal" Width="40" Height="40" Stretch="UniformToFill" RenderTransformOrigin="0.5,0.5">
                            <Image.Clip>
                                <EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/>
                            </Image.Clip>
                            <Image.RenderTransform>
                                <RotateTransform x:Name="AlbumArtMinimalRotation" Angle="0"/>
                            </Image.RenderTransform>
                        </Image>
                    </Grid>
                    
                    <Grid Grid.Column="1" x:Name="GifMinimalContainer" Visibility="Collapsed" Margin="5,0,0,0" Width="50" Height="50">
                        <Ellipse Width="50" Height="50" Fill="Gray"/>
                        <Image x:Name="GifMinimal" Width="40" Height="40" Stretch="UniformToFill" gif:ImageBehavior.AnimatedSource="flex.gif">
                            <Image.Clip>
                                <EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/>
                            </Image.Clip>
                        </Image>
                    </Grid>
                </Grid>
            </Grid>
            
            <!-- Компактный режим - иконка + текст + гифка -->
            <Grid x:Name="CompactMode" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="6"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" ClipToBounds="True">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Слайд-контейнер: старый контент -->
                    <Grid x:Name="CompactContentOld" Grid.Column="0" Opacity="1">
                        <Grid.RenderTransform><TranslateTransform x:Name="CompactOldTranslate"/></Grid.RenderTransform>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Margin="5,0,0,0">
                            <Ellipse Width="50" Height="50" Fill="Gray"/>
                            <Image x:Name="AlbumArtOld" Width="40" Height="40" Stretch="UniformToFill">
                                <Image.Clip><EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/></Image.Clip>
                            </Image>
                        </Grid>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                            <TextBlock x:Name="CompactTitleOld" Foreground="White" FontSize="11" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                            <TextBlock x:Name="CompactArtistOld" Foreground="#AAAAAA" FontSize="9" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Grid>

                    <!-- Слайд-контейнер: новый контент -->
                    <Grid x:Name="CompactContentNew" Grid.Column="0" Opacity="1">
                        <Grid.RenderTransform><TranslateTransform x:Name="CompactNewTranslate"/></Grid.RenderTransform>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Margin="5,0,0,0">
                            <Ellipse Width="50" Height="50" Fill="Gray"/>
                            <Image x:Name="AlbumArt" Width="40" Height="40" Stretch="UniformToFill" RenderTransformOrigin="0.5,0.5">
                                <Image.Clip><EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/></Image.Clip>
                                <Image.RenderTransform><RotateTransform x:Name="AlbumArtRotation" Angle="0"/></Image.RenderTransform>
                            </Image>
                        </Grid>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                            <TextBlock x:Name="CompactTitle" Text="Название" Foreground="White" FontSize="11" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                            <TextBlock x:Name="CompactArtist" Text="Исполнитель" Foreground="#AAAAAA" FontSize="9" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Column="1" x:Name="GifCompactContainer" Visibility="Collapsed" Margin="0,0,5,0" Width="50" Height="50">
                        <Ellipse Width="50" Height="50" Fill="Gray"/>
                        <Image x:Name="GifCompact" Width="40" Height="40" Stretch="UniformToFill" gif:ImageBehavior.AnimatedSource="flex.gif">
                            <Image.Clip><EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/></Image.Clip>
                        </Image>
                    </Grid>
                </Grid>

                <!-- Прогресс-бар -->
                <Grid Grid.Row="1" Margin="8,0,8,1" VerticalAlignment="Bottom">
                    <Rectangle x:Name="ProgressTrackCompact" Height="2" Fill="#444444" RadiusX="1" RadiusY="1" HorizontalAlignment="Stretch"/>
                    <Rectangle x:Name="ProgressBarCompact" Height="2" Fill="White" RadiusX="1" RadiusY="1" HorizontalAlignment="Left" Width="0"/>
                    <Ellipse x:Name="ProgressDotCompact" Width="6" Height="6" Fill="White" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,0,0,0"/>
                </Grid>
            </Grid>
            
            <!-- Расширенный режим -->
            <Grid x:Name="ExpandedMode" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="6"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" ClipToBounds="True">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Слайд-контейнер: старый контент -->
                    <Grid x:Name="ExpandedContentOld" Grid.Column="0" Opacity="1">
                        <Grid.RenderTransform><TranslateTransform x:Name="ExpandedOldTranslate"/></Grid.RenderTransform>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Margin="5,0,0,0">
                            <Ellipse Width="50" Height="50" Fill="Gray"/>
                            <Image x:Name="AlbumArtExpandedOld" Width="40" Height="40" Stretch="UniformToFill">
                                <Image.Clip><EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/></Image.Clip>
                            </Image>
                        </Grid>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                            <TextBlock x:Name="TrackTitleOld" Foreground="White" FontSize="12" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                            <TextBlock x:Name="ArtistNameOld" Foreground="#AAAAAA" FontSize="10" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Grid>

                    <!-- Слайд-контейнер: новый контент -->
                    <Grid x:Name="ExpandedContentNew" Grid.Column="0" Opacity="1">
                        <Grid.RenderTransform><TranslateTransform x:Name="ExpandedNewTranslate"/></Grid.RenderTransform>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Margin="5,0,0,0">
                            <Ellipse Width="50" Height="50" Fill="Gray"/>
                            <Image x:Name="AlbumArtExpanded" Width="40" Height="40" Stretch="UniformToFill" RenderTransformOrigin="0.5,0.5">
                                <Image.Clip><EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/></Image.Clip>
                                <Image.RenderTransform><RotateTransform x:Name="AlbumArtExpandedRotation" Angle="0"/></Image.RenderTransform>
                            </Image>
                        </Grid>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                            <TextBlock x:Name="TrackTitle" Text="Название трека" Foreground="White" FontSize="12" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                            <TextBlock x:Name="ArtistName" Text="Исполнитель" Foreground="#AAAAAA" FontSize="10" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Column="1" x:Name="GifExpandedContainer" Visibility="Collapsed" Margin="0,0,5,0" Width="50" Height="50">
                        <Ellipse Width="50" Height="50" Fill="Gray"/>
                        <Image x:Name="GifExpanded" Width="40" Height="40" Stretch="UniformToFill" gif:ImageBehavior.AnimatedSource="flex.gif">
                            <Image.Clip><EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/></Image.Clip>
                        </Image>
                    </Grid>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button x:Name="PrevButton" Content="⏮" Style="{StaticResource ControlButtonStyle}" Click="PrevButton_Click"/>
                        <Button x:Name="PlayPauseButton" Content="⏸" Style="{StaticResource ControlButtonStyle}" Click="PlayPauseButton_Click"/>
                        <Button x:Name="NextButton" Content="⏭" Style="{StaticResource ControlButtonStyle}" Click="NextButton_Click"/>
                    </StackPanel>
                </Grid>

                <!-- Прогресс-бар -->
                <Grid Grid.Row="1" Margin="8,0,8,1" VerticalAlignment="Bottom">
                    <Rectangle x:Name="ProgressTrackExpanded" Height="2" Fill="#444444" RadiusX="1" RadiusY="1" HorizontalAlignment="Stretch"/>
                    <Rectangle x:Name="ProgressBarExpanded" Height="2" Fill="White" RadiusX="1" RadiusY="1" HorizontalAlignment="Left" Width="0"/>
                    <Ellipse x:Name="ProgressDotExpanded" Width="6" Height="6" Fill="White" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,0,0,0"/>
                </Grid>
            </Grid>
        </Grid>
        </Border>
    </Grid>
</Window>
